This repo contains the Docker-based configuration for my personal server plus other useful scripts.

🚀 Services Deployed
Pihole
nginx
Vaultwarden


File structure

homelab/
├── pihole/
│   └── docker-compose.yml
├── npm/
│   └── docker-compose.yml
└── vaultwarden/
    └── docker-compose.yml
